---
const { class: className, ...props } = Astro.props;
---

<div {...props} class:list={[className, 'glass']}>
  <slot />
</div>
<style>
  div {
    @apply container p-2 sm:p-5 flex flex-col gap-2 sm:gap-8
    border border-primary-light/50 dark:border-primary-dark/50
    max-sm:max-w-[calc(100%-10px)];
  }
</style>
