---
const { href, class: className, ...props } = Astro.props;
const { pathname } = Astro.url;
const isActive = decodeURI(pathname).includes(href);
---
<a href={href} class:list={[className, { active: isActive }]} {...props}><slot /></a>
