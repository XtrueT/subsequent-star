---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time class="pubDate" datetime={date.toISOString()}>
  {
    date.toLocaleDateString("zh-cn", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
