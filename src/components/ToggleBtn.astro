---
const { id, text } = Astro.props;
---

<div class="relative inline-block group acrylic">
  <button
    id={id}
    class="transition-colors p-2 flex items-center
            hover:bg-purple-500 hover:text-slate-50 dark:hover:bg-purple-500"
  >
    <slot />
  </button>

  <p
    class="absolute bg-inherit items-center justify-center w-20 p-1 -mx-2 rounded-lg shadow-lg -left-[185%] top-[10%] hidden group-hover:flex"
  >
    <span class="truncate">{text}</span>
  </p>
</div>
